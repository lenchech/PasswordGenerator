char* toLowerCase(char* str) {
    int i, j, k = 0;
    int Lstr = strlen(str);
    for(i = 0; i < Lstr; i++){
        if(str[i] >= 'A' && str[i] <= 'Z'){
            str[i] = str[i] + 32;
        }
    }
    return str;
}
